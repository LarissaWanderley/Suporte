@model IList<Suporte.Dominio.Marca>
<label class="label  btn-danger">@ViewBag.Error</label>
<table class="table table-condensed table-striped table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>@Html.ActionLink("Incluir", "Form", "Marca", new { id = 0 }, new { @class = "btn btn-info btn-xs  pull-right" })</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var marca in Model)
            {
            <tr>
                <td>@marca.Id</td>
                <td>@Html.ActionLink(marca.TxMarca, "Form", new { id = marca.Id })</td>
                <td>@Html.ActionLink("Excluir", "Excluir", "Marca", new { id = marca.Id }, new { @class = "btn btn-info btn-xs  pull-right", @onclick = "JavaScript:return(confirm('Confirma a exclusão do registro?'))" })</td>
            </tr>
        }
    </tbody>
</table>